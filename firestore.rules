rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV: iedereen mag lezen, schrijven alleen ingelogd
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
